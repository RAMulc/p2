
<nav class="navbar fixed-top">
        <a class="navbar-brand text-white" href="#">Eat Well - Live Well</a>
        <span class="navbar-text text-white pr-3">
            <span class="member-fname"></span>
            <span class="member-lname"></span>
    </nav>

    <div class="container-fluid">
         <div class="row">
             <aside class="col-2 px-0" id="left">
                <div class="list-group w-100">
                    <a href="#" id="homeButton" class="list-group-item active">Home</a>
                    <a href="#" id="addRecipeButton" class="list-group-item">Add Recipe</a>
                    <a href="#" id="logoutButton" class="list-group-item" data-toggle="modal" data-target="#logoutModal">Logout</a>
                </div>

             </aside>
           
            <main class="col-10 offset-2" id="right">
                <div class="row bg-white m-3">
                    <div class="col-12 py-4">
                        <div id="homeScreen">{{> home}}</div>
                        <div id="addrecipeScreen">{{> addrecipe}}</div>
                        <div id="cookmeScreen">{{> cookme}}</div>
                    </div>
                </div>
               <!-- Modal -->
<div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="logoutModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="logoutModalLabel">Leaving So Soon!</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Are you sure you want to logout?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Take me back</button>
        <button type="button" class="btn btn-success" id="modalLogoutButton">Logout</button>
      </div>
    </div>
  </div>
</div>

            </main>
         </div>
    </div>
